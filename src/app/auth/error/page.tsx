import React, { FormEvent, useRef } from "react";

const Error = () => {

    return (
        <div className="auth">
            A server error occured.
        </div>
    );
}

export default Error;